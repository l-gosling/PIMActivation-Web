# Entra ID OAuth Configuration
ENTRA_TENANT_ID=your-tenant-id.onmicrosoft.com
ENTRA_CLIENT_ID=your-app-client-id
ENTRA_CLIENT_SECRET=your-app-client-secret
ENTRA_REDIRECT_URI=https://{fqdn}/api/auth/callback

# HTTPS Configuration
# Port mapped on the host (default 443)
HTTPS_PORT=443

# Pode Server Configuration
PODE_PORT=8080
PODE_MODE=production

# Pode request thread count (runspaces serving HTTP requests).
# Pode uses PowerShell runspaces; each ~50-100 MB RAM at baseline.
# Workload here is I/O-bound (Graph API calls), so threads > CPU cores is fine.
#   10     — default, handles ~5-10 concurrent users comfortably
#   20-30  — recommended for 10-50 concurrent users
#   50     — useful max for heavy multi-user loads (50+ concurrent)
#   >50    — diminishing returns; runspace overhead dominates. Hard ceiling ~100.
PODE_THREADS=10

# Rate limiting: max requests per IP per 60 seconds. Set to 0 to disable.
# Defends /api/auth/* against brute-force and OAuth-state fuzzing. 120 is generous
# for interactive UI use (each page load fires several API calls).
RATE_LIMIT_PER_MINUTE=120

# Role-policy cache TTL in seconds. Policy data is tenant-wide and rarely changes,
# so caching saves one Graph API call per role-view. Set to 0 to disable.
POLICY_CACHE_TTL=600

# Logging
LOG_LEVEL=Information

# Session Configuration
SESSION_TIMEOUT=43200

# Feature Flags
INCLUDE_ENTRA_ROLES=true
INCLUDE_GROUPS=true
INCLUDE_AZURE_RESOURCES=false
# Enable Entra audit log sync in history (requires AuditLog.Read.All permission)
INCLUDE_AUDIT_LOGS=true

# Custom Theme (optional — defaults to Microsoft Fluent Design colors)
# THEME_PRIMARY_COLOR=#0078D4
# THEME_SECONDARY_COLOR=#107C10
# THEME_DANGER_COLOR=#DA3B01
# THEME_WARNING_COLOR=#FFB900
# THEME_SUCCESS_COLOR=#107C10
# THEME_FONT_FAMILY=Segoe UI, -apple-system, sans-serif
# THEME_SECTION_HEADER_COLOR=#0078D4
# THEME_ENTRA_COLOR=#0078D4
# THEME_GROUP_COLOR=#107C10
# THEME_AZURE_COLOR=#003067

# Copyright text shown in footer (optional — hidden if empty)
# APP_COPYRIGHT=© 2026 Your Organization. All rights reserved.

# Graph API Configuration
GRAPH_API_TIMEOUT=30000
GRAPH_BATCH_SIZE=20
